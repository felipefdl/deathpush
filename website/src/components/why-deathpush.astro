<section class="workflow section">
  <div class="container">
    <div class="workflow-header">
      <h2 class="text-heading">Your agent codes. You stay in control.</h2>
      <p class="text-subheading">
        AI agents write hundreds of lines while you wait. DeathPush shows you every change as it lands.
      </p>
    </div>
    <div class="workflow-steps">
      <div class="step">
        <div class="step-icon-wrap">
          <div class="step-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/></svg>
          </div>
          <span class="step-number">1</span>
        </div>
        <div class="step-content">
          <h3 class="step-title">Ship from your terminal</h3>
          <p class="step-desc">Run Claude Code, Codex, Vim, whatever. Your terminal is your editor. Keep it that way.</p>
        </div>
      </div>
      <div class="step-connector" aria-hidden="true">
        <div class="connector-line"></div>
        <svg class="connector-arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 6 15 12 9 18"/></svg>
      </div>
      <div class="step">
        <div class="step-icon-wrap">
          <div class="step-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
          </div>
          <span class="step-number">2</span>
        </div>
        <div class="step-content">
          <h3 class="step-title">Watch changes land</h3>
          <p class="step-desc">DeathPush picks up file changes in real time. Diffs, staging tree, file explorer, all updating live.</p>
        </div>
      </div>
      <div class="step-connector" aria-hidden="true">
        <div class="connector-line"></div>
        <svg class="connector-arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 6 15 12 9 18"/></svg>
      </div>
      <div class="step">
        <div class="step-icon-wrap">
          <div class="step-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          <span class="step-number">3</span>
        </div>
        <div class="step-content">
          <h3 class="step-title">Review, tweak, commit</h3>
          <p class="step-desc">Stage what's right. Fix a typo inline. Commit when you're ready. No IDE needed.</p>
        </div>
      </div>
    </div>
    <div class="works-with">
      <p class="works-with-label">Works with any terminal workflow</p>
      <div class="tools-list">
        <span class="tool-name">Claude Code</span>
        <span class="tool-name">Codex</span>
        <span class="tool-name">Vim / Neovim</span>
        <span class="tool-name">Copilot CLI</span>
        <span class="tool-name">OpenCode</span>
        <span class="tool-name">Aider</span>
        <span class="tool-name">any CLI agent</span>
      </div>
    </div>
  </div>
</section>

<style>
  .workflow-header {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }

  .workflow-header h2 {
    margin-bottom: var(--space-md);
  }

  .workflow-steps {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    max-width: 720px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .workflow-steps {
      flex-direction: row;
      align-items: flex-start;
      gap: 0;
      max-width: none;
    }
  }

  /* ── Step card ── */
  .step {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    opacity: 0;
    animation: step-in 0.5s ease-out forwards;
  }

  .step:nth-child(1) { animation-delay: 0.1s; }
  .step:nth-child(3) { animation-delay: 0.3s; }
  .step:nth-child(5) { animation-delay: 0.5s; }

  @keyframes step-in {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .step-icon-wrap {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .step-icon {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-accent);
    background: var(--color-accent-glow);
    border-radius: var(--radius-md);
    flex-shrink: 0;
  }

  .step-number {
    font-size: var(--text-xs);
    font-weight: 700;
    font-family: var(--font-mono);
    color: var(--color-text-muted);
    width: 22px;
    height: 22px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-full);
  }

  .step-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .step-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-text);
  }

  .step-desc {
    font-size: var(--text-sm);
    line-height: 1.6;
    color: var(--color-text-secondary);
  }

  /* ── Connector between steps ── */
  .step-connector {
    display: none;
  }

  @media (min-width: 768px) {
    .step-connector {
      display: flex;
      align-items: center;
      padding: 0 var(--space-sm);
      padding-top: 10px;
      flex-shrink: 0;
      opacity: 0;
      animation: connector-in 0.4s ease-out forwards;
    }

    .step-connector:nth-child(2) { animation-delay: 0.25s; }
    .step-connector:nth-child(4) { animation-delay: 0.45s; }
  }

  @keyframes connector-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  .connector-line {
    width: 24px;
    height: 1px;
    background: var(--color-border);
  }

  .connector-arrow {
    color: var(--color-text-muted);
    flex-shrink: 0;
    animation: arrow-pulse 2.5s ease-in-out infinite;
  }

  @keyframes arrow-pulse {
    0%, 100% { opacity: 0.4; }
    50% { opacity: 1; }
  }

  /* ── Works with ── */
  .works-with {
    margin-top: var(--space-3xl);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  .works-with-label {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-text-muted);
    margin-bottom: var(--space-md);
  }

  .tools-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: var(--space-sm);
  }

  .tool-name {
    font-size: var(--text-sm);
    font-family: var(--font-mono);
    color: var(--color-text-muted);
    padding: var(--space-xs) var(--space-sm);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    white-space: nowrap;
    transition: border-color 0.3s, color 0.3s, box-shadow 0.3s;
  }

  .tool-name:hover {
    color: var(--color-accent);
    border-color: rgba(232, 217, 189, 0.35);
    box-shadow: 0 0 12px rgba(232, 217, 189, 0.08);
  }
</style>
