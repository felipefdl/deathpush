<section class="hero section">
  <div class="container hero-inner">
    <p class="hero-brand">DeathPush</p>
    <h1 class="text-hero hero-title">Murder the Noise<br />Ship the Code</h1>
    <p class="text-subheading hero-sub">
      Your AI agent writes the code. You keep eyes on every change.<br class="hide-mobile" />
      A Git GUI that sits next to your terminal, not instead of it.
    </p>
    <div class="cta-group">
      <a
        href="https://github.com/felipefdl/deathpush"
        target="_blank"
        rel="noopener noreferrer"
        class="btn btn-primary"
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
        </svg>
        View on GitHub
      </a>
    </div>
    <div class="mockup">
      <div class="mockup-window" id="mockup-window">
        <div class="mockup-titlebar">
          <div class="titlebar-mac">
            <span class="dot red"></span>
            <span class="dot yellow"></span>
            <span class="dot green"></span>
          </div>
          <span class="titlebar-text">DeathPush - main</span>
        </div>
        <div class="mockup-body">
          <div class="mockup-sidebar">
            <div class="sidebar-input"></div>
            <div class="sidebar-btn"></div>
            <div class="sidebar-label">STAGED CHANGES</div>
            <div class="sidebar-file"><span class="file-dot green"></span><span class="line w70"></span><span class="badge-m">M</span></div>
            <div class="sidebar-file"><span class="file-dot green"></span><span class="line w50"></span><span class="badge-m">M</span></div>
            <div class="sidebar-file"><span class="file-dot green"></span><span class="line w80"></span><span class="badge-a">A</span></div>
            <div class="sidebar-file"><span class="file-dot green"></span><span class="line w60"></span><span class="badge-m">M</span></div>
            <div class="sidebar-label">CHANGES</div>
            <div class="sidebar-file"><span class="file-dot red"></span><span class="line w55"></span><span class="badge-m">M</span></div>
            <div class="sidebar-file"><span class="file-dot red"></span><span class="line w75"></span><span class="badge-u">U</span></div>
            <div class="sidebar-file"><span class="file-dot red"></span><span class="line w45"></span><span class="badge-u">U</span></div>
          </div>
          <div class="mockup-diff">
            <div class="diff-header"><span class="line w40"></span></div>
            <div class="diff-cols">
              <div class="diff-col">
                <div class="diff-line"><span class="ln">12</span><span class="code w90"></span></div>
                <div class="diff-line"><span class="ln">13</span><span class="code w60"></span></div>
                <div class="diff-line removed"><span class="ln">14</span><span class="code w75"></span></div>
                <div class="diff-line removed"><span class="ln">15</span><span class="code w50"></span></div>
                <div class="diff-line"><span class="ln">16</span><span class="code w85"></span></div>
                <div class="diff-line"><span class="ln">17</span><span class="code w40"></span></div>
                <div class="diff-line"><span class="ln">18</span><span class="code w70"></span></div>
                <div class="diff-line removed"><span class="ln">19</span><span class="code w55"></span></div>
                <div class="diff-line"><span class="ln">20</span><span class="code w80"></span></div>
                <div class="diff-line"><span class="ln">21</span><span class="code w45"></span></div>
              </div>
              <div class="diff-col">
                <div class="diff-line"><span class="ln">12</span><span class="code w90"></span></div>
                <div class="diff-line"><span class="ln">13</span><span class="code w60"></span></div>
                <div class="diff-line added"><span class="ln">14</span><span class="code w80"></span></div>
                <div class="diff-line added"><span class="ln">15</span><span class="code w65"></span></div>
                <div class="diff-line added"><span class="ln">16</span><span class="code w45"></span></div>
                <div class="diff-line"><span class="ln">17</span><span class="code w85"></span></div>
                <div class="diff-line"><span class="ln">18</span><span class="code w40"></span></div>
                <div class="diff-line"><span class="ln">19</span><span class="code w70"></span></div>
                <div class="diff-line"><span class="ln">20</span><span class="code w80"></span></div>
                <div class="diff-line added"><span class="ln">21</span><span class="code w60"></span></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


<style>
  .hero {
    position: relative;
    padding-top: calc(var(--header-height) + var(--space-4xl));
    text-align: center;
    overflow: hidden;
  }

  .hero::before {
    content: "";
    position: absolute;
    top: calc(var(--header-height) + var(--space-lg));
    left: 50%;
    transform: translateX(-50%);
    width: 480px;
    height: 480px;
    background-image: url("/logo-white.png");
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.04;
    pointer-events: none;
    z-index: 0;
    mask-image: linear-gradient(
      135deg,
      transparent 0%,
      transparent 35%,
      rgba(255, 255, 255, 0.6) 38%,
      white 40%,
      rgba(255, 255, 255, 0.6) 42%,
      transparent 45%,
      transparent 100%
    );
    -webkit-mask-image: linear-gradient(
      135deg,
      transparent 0%,
      transparent 35%,
      rgba(255, 255, 255, 0.6) 38%,
      white 40%,
      rgba(255, 255, 255, 0.6) 42%,
      transparent 45%,
      transparent 100%
    );
    mask-size: 300% 300%;
    -webkit-mask-size: 300% 300%;
    animation: shine 6s ease-in-out infinite;
  }

  @keyframes shine {
    0%, 100% {
      mask-position: 100% 100%;
      -webkit-mask-position: 100% 100%;
    }
    50% {
      mask-position: 0% 0%;
      -webkit-mask-position: 0% 0%;
    }
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-lg);
  }

  .hero-brand {
    font-size: var(--text-sm);
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--color-accent);
  }

  .hero-title {
    max-width: 800px;
    font-weight: 700;
  }

  .hero-sub {
    max-width: 780px;
  }

  .hide-mobile {
    display: none;
  }

  @media (min-width: 768px) {
    .hide-mobile {
      display: inline;
    }
  }

  .cta-group {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-md);
    margin-top: var(--space-md);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: 0.75rem 1.5rem;
    font-size: var(--text-base);
    font-weight: 600;
    border-radius: var(--radius-md);
    transition: all 0.2s;
  }

  .btn-primary {
    color: var(--color-accent-text);
    background: linear-gradient(to bottom, var(--color-accent), var(--color-accent-dark));
    box-shadow: 0 0 20px var(--color-accent-glow);
  }

  .btn-primary:hover {
    background: var(--color-accent-hover);
    box-shadow: 0 0 30px rgba(232, 217, 189, 0.3);
  }

  /* Mockup */
  .mockup {
    position: relative;
    width: 100%;
    max-width: 900px;
    margin-top: var(--space-2xl);
  }

  .mockup::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(to bottom, transparent, var(--color-bg));
    pointer-events: none;
    z-index: 2;
    border-radius: 0 0 var(--radius-lg) var(--radius-lg);
  }

  .mockup-window {
    position: relative;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 0 60px rgba(232, 217, 189, 0.06), 0 20px 60px rgba(0, 0, 0, 0.5);
    animation: mockup-float 6s ease-in-out infinite;
  }

  @keyframes mockup-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }

  .mockup-titlebar {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 14px;
    z-index: 1;
  }

  /* macOS dots */
  .titlebar-mac {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .dot.red { background: #ff5f57; }
  .dot.yellow { background: #febc2e; }
  .dot.green { background: #28c840; }

  .titlebar-text {
    position: absolute;
    left: 0;
    right: 0;
    text-align: center;
    font-size: 11px;
    color: var(--color-text-muted);
    pointer-events: none;
  }

  .mockup-body {
    display: flex;
    background: #0d0d0d;
    height: 320px;
    padding-top: 36px;
  }

  /* Sidebar */
  .mockup-sidebar {
    width: 220px;
    border-right: 1px solid var(--color-border);
    padding: 10px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    flex-shrink: 0;
  }

  .sidebar-input {
    height: 26px;
    border-radius: 4px;
    border: 1px solid var(--color-border);
    background: #141414;
    margin-bottom: 2px;
  }

  .sidebar-btn {
    height: 28px;
    border-radius: 4px;
    background: var(--color-accent);
    opacity: 0.8;
    margin-bottom: 6px;
  }

  .sidebar-label {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--color-text-muted);
    padding: 4px 0 2px;
  }

  .sidebar-file {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 3px 4px;
    border-radius: 3px;
    animation: file-appear 0.3s ease-out both;
  }

  .sidebar-file:nth-child(1) { animation-delay: 0.5s; }
  .sidebar-file:nth-child(2) { animation-delay: 0.6s; }
  .sidebar-file:nth-child(3) { animation-delay: 0.7s; }
  .sidebar-file:nth-child(4) { animation-delay: 0.8s; }
  .sidebar-file:nth-child(5) { animation-delay: 0.9s; }
  .sidebar-file:nth-child(6) { animation-delay: 1.0s; }
  .sidebar-file:nth-child(7) { animation-delay: 1.1s; }

  @keyframes file-appear {
    from { opacity: 0; transform: translateY(4px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .file-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .file-dot.green { background: #28c840; }
  .file-dot.red { background: #e5c07b; }

  .badge-m, .badge-a, .badge-u {
    font-size: 8px;
    font-weight: 700;
    border-radius: 2px;
    padding: 0 3px;
    margin-left: auto;
    flex-shrink: 0;
  }

  .badge-m { color: #f59e0b; }
  .badge-a { color: #28c840; }
  .badge-u { color: var(--color-text-muted); }

  /* Shared line block */
  .line {
    display: block;
    height: 6px;
    border-radius: 3px;
    background: #222;
  }

  /* Diff area */
  .mockup-diff {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  .diff-header {
    padding: 8px 12px;
    border-bottom: 1px solid var(--color-border);
    background: #111;
  }

  .diff-header .line {
    height: 8px;
    background: #1e1e1e;
  }

  .diff-cols {
    display: flex;
    flex: 1;
  }

  .diff-col {
    flex: 1;
    padding: 6px 0;
    display: flex;
    flex-direction: column;
    gap: 1px;
  }

  .diff-col + .diff-col {
    border-left: 1px solid var(--color-border);
  }

  .diff-line {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 2px 10px;
    height: 24px;
    animation: line-appear 0.4s ease-out both;
  }

  .diff-line:nth-child(1) { animation-delay: 0.3s; }
  .diff-line:nth-child(2) { animation-delay: 0.4s; }
  .diff-line:nth-child(3) { animation-delay: 0.5s; }
  .diff-line:nth-child(4) { animation-delay: 0.6s; }
  .diff-line:nth-child(5) { animation-delay: 0.7s; }
  .diff-line:nth-child(6) { animation-delay: 0.8s; }
  .diff-line:nth-child(7) { animation-delay: 0.9s; }
  .diff-line:nth-child(8) { animation-delay: 1.0s; }
  .diff-line:nth-child(9) { animation-delay: 1.1s; }
  .diff-line:nth-child(10) { animation-delay: 1.2s; }

  @keyframes line-appear {
    from { opacity: 0; transform: translateX(8px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .diff-line.added {
    background: rgba(34, 197, 94, 0.08);
    border-left: 2px solid rgba(34, 197, 94, 0.5);
  }

  .diff-line.removed {
    background: rgba(220, 38, 38, 0.08);
    border-left: 2px solid rgba(220, 38, 38, 0.5);
  }

  .ln {
    font-size: 9px;
    color: #333;
    width: 18px;
    text-align: right;
    flex-shrink: 0;
    font-family: var(--font-mono);
  }

  .code {
    display: block;
    height: 5px;
    border-radius: 2px;
    background: #1e1e1e;
  }

  .added .code { background: rgba(34, 197, 94, 0.2); }
  .removed .code { background: rgba(220, 38, 38, 0.2); }

  /* Width utilities */
  .w40 { width: 40%; }
  .w45 { width: 45%; }
  .w50 { width: 50%; }
  .w55 { width: 55%; }
  .w60 { width: 60%; }
  .w65 { width: 65%; }
  .w70 { width: 70%; }
  .w75 { width: 75%; }
  .w80 { width: 80%; }
  .w85 { width: 85%; }
  .w90 { width: 90%; }

  @media (max-width: 767px) {
    .mockup-sidebar {
      width: 140px;
    }

    .mockup-body {
      height: 240px;
    }

    .diff-cols {
      flex-direction: column;
    }

    .diff-col + .diff-col {
      border-left: none;
      border-top: 1px solid var(--color-border);
    }
  }
</style>
